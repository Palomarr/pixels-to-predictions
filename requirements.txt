# Pinned versions for non-Kaggle reproduction. Kaggle's GPU image already
# carries most of these; the training notebook re-pins these exact versions
# via `pip install --upgrade ...` so behavior is consistent across runs.
#
# For local reproduction (e.g., RTX 4090 + CUDA 12.x):
#   pip install -r requirements.txt
#   python smoke_test.py --data_dir data
#   python train.py --ablation v3_epochs5 --data_dir data

torch>=2.6,<3.0          # PyTorch 2.10 on Kaggle; >=2.6 covers the AutoModelForImageTextToText API
transformers==4.57.6     # pinned to match training; v5+ removes AutoModelForVision2Seq
peft==0.18.1             # for use_dora=True support and stable LoRA targeting
bitsandbytes>=0.43       # 4-bit NF4 quantization at training time only (offline submission falls back to bf16)
accelerate>=0.34         # required by HF Trainer
safetensors>=0.4         # adapter serialization
pandas>=2.0
pillow>=10.0
tqdm>=4.66
